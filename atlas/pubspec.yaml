name: fluttergames
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.0-102.0.dev <4.0.0'

dependencies:
  firebase_core: ^2.24.2
  flutter:
    sdk: flutter
    
  google_books_api: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  material_design_icons_flutter: ^6.2.0

flutter:
  uses-material-design: true
